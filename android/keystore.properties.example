# 이 파일을 keystore.properties 로 복사해 값을 채운다 (그 파일은 커밋 안 된다).
# 열쇠는 TWA 때 쓰던 것 그대로여야 한다 — 다른 열쇠로 서명하면 스토어가 거절한다.
storeFile=../../android-build/android.keystore
storePassword=여기에 키스토어 비밀번호
keyAlias=flipflap
keyPassword=여기에 키 비밀번호
